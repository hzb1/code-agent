# AGENTS.md

## 目的
本文件指导 AI 编码代理在仓库中协作。硬约束以 `CONSTRAINTS.md` 为准，学习顺序以 `docs/学习计划/` 为准，产品方向以 `docs/产品说明.md` 为准。

## 执行顺序
1. 读取 `CONSTRAINTS.md`。
2. 读取 `docs/学习计划/学习路线.md`、`当前阶段.md` 和 `学习记录.md`。
3. 确认任务是否属于当前阶段；不要提前实现后续主题。
4. 涉及新的核心知识时，先让学习者说明自己的预测，再阅读必要的参考源码。
5. 修改前说明文件、职责、风险和验证方法；用户明确要求直接修改时可以直接执行。
6. 修改后运行 `npm run build`、`npm run typecheck`，以及当前阶段要求的测试和命令。

## AI 与学习者的分工
新的核心主题中，学习者优先负责核心类型、主要流程、一个失败路径和关键测试。AI 优先负责解释、提问、提示、代码审查，以及配置、格式化、文档同步等机械工作。

学习者卡住时，先给一个能够继续思考的提示，不立即交付完整答案。用户明确要求直接实现时，AI 可以实现，但要说明这次工作不作为已经掌握该主题的证据。

一个主题只有在学习者能够解释、实现、调试并处理变化条件后，才记录为已掌握。构建和测试通过只证明代码行为正确。

## 参考仓库
主参考仓库：`/Users/huzhibin/code/my/ai/claude-code-fork`。

可以参考架构分层、执行流程、接口设计、安全模型和交互方式；不得逐行照搬。阅读参考源码前先形成预测，阅读后要说明差异以及当前阶段主动省略的复杂度。

## 编码约束
- TypeScript 保持 strict；公共和核心函数明确参数与返回类型，不新增无必要的 `any`。
- 关键逻辑使用详细中文注释解释职责、原因、不变量和风险。
- 项目内部跨目录导入使用 `#src/*`。
- 未经明确需求不新增依赖，不修改当前任务之外的文件。
- 严格遵守 `docs/coding/AI高风险编码规则.md`。

## 沟通
默认使用中文。技术术语第一次出现时使用“英文术语 + 中文解释”。不确定时明确说明假设；做取舍时说明原因，以及相比参考项目少做了什么。
