# AGENTS.md

## 目的
本文件用于指导 AI 编码代理（如 Codex）在本仓库内稳定协作。

本仓库的主目标已经明确：
- 以本地参考项目 `claude-code-fork` 为主参考，逐步做出同类本地 CLI Coding Agent；
- 在工程实现层优先学习和借鉴参考项目的骨架、分层、执行链路与安全模型；
- 在交互体验层分开参考：命令行体验优先参考 `claude-code`，桌面端体验在未来阶段参考 Mac 版 `Codex`；
- 当前主形态是 CLI，因此当前阶段优先参考 `claude-code` 的命令行体验。

当前开发环境中的参考仓库路径：
- `/Users/huzhibin/code/my/ai/claude-code-fork`

关于这个参考仓库，要明确一点：
- `claude-code-fork` 不是普通“灵感参考项目”；
- 它是一个针对 Anthropic 官方 `Claude Code` CLI 的源码反编译 / 逆向还原项目；
- 因此，在本仓库里提到“参考项目”时，默认指的是一个可直接阅读、可直接学习的源码级主参考。

约束规则来源：`CONSTRAINTS.md`。
版本执行来源：`docs/version-plan/`。

---

## 文档优先级
出现冲突时，按以下顺序决策：
1. `CONSTRAINTS.md`
2. `AGENTS.md`
3. `docs/version-plan/版本总览.md`
4. 当前目标版本文档（例如 `docs/version-plan/v0.1.0.md`）
5. `docs/产品说明.md`
6. `README.md`
7. 现有实现代码

关键原则：
- 现有实现不是最高优先级；
- 如果现有实现与参考项目主线、当前目标版本、当前约束冲突，可以重构甚至推翻；
- 但重构必须服从当前版本目标，不能跳版本乱做。

---

## 工作流（最高优先级）
- 不要直接写代码，必须先规划，再编码。
- 默认执行顺序：
  1. 读取 `CONSTRAINTS.md`。
  2. 读取 `docs/version-plan/版本总览.md`。
  3. 读取当前目标版本文档。
  4. 读取 `docs/产品说明.md`，确认产品定位与阶段目标。
  5. 阅读本仓库现有实现与必要的参考仓库源码。
  6. 给出简明计划：改哪些文件、为什么改、风险点、验证方式。
  7. 与用户对齐后再编码。
  8. 编码后执行验证并汇报结果。
- 仅当用户明确要求“直接改代码”时，才可跳过“先给计划”，但改完后仍需补回：
  - 改动说明
  - 风险说明
  - 验证结果

---

## 执行清单（每次任务都做）
1. 先确认本轮对应哪个版本目标。
2. 再确认本轮是否需要阅读参考仓库中的相关源码或文档。
3. 改动时优先对齐参考项目骨架，不优先保留旧实现形状。
4. 只做当前版本需要的能力，不提前透支后续版本。
5. 做最小可行改动，但如果旧结构已经明显阻碍目标达成，可以进行必要重构。
6. 优先解决：
   - 安全边界
   - 架构边界
   - 稳定性
   - 可诊断性
7. 修改后必须验证：
   - `npm run build`
   - `npm run typecheck`
8. 若当前版本文档中还有额外验证目标，也要一并验证。
9. 编码时严格遵守注释约束：关键逻辑必须写详细中文注释，尤其是“为什么这样做”和“这一步服务于哪一层架构”。
10. 汇报格式：先结果，后改动点，最后写验证结果、风险和未完成项。
11. 严格遵守 AI 高风险编码规则（见 `docs/coding/AI高风险编码规则.md`），重点避免隐式副作用、边界越权、过度抽象、类型放水和错误处理偷懒。

---

## 参考仓库使用规则
- 参考仓库是“主参考”，不是“可有可无的灵感来源”。
- 做重大设计变更前，应优先查阅参考仓库对应部分，例如：
  - 入口与命令分发：`src/entrypoints/cli.tsx`、`src/main.tsx`
  - 单轮循环：`src/query.ts`
  - 会话编排：`src/QueryEngine.ts`
  - 工具系统：`src/Tool.ts`、`src/tools.ts`
  - 上下文：`src/context.ts`
  - 流式适配：`src/services/api/openai/*`
  - 文档：`docs/`
- 可以借鉴：
  - 架构分层
  - 执行流程
  - 接口设计思路
  - 安全模型
  - 可观测性与交互方式
- 不可以做：
  - 逐行照搬源码
  - 不加理解地复制内部细节
  - 为了“看起来像参考项目”而引入当前版本不需要的复杂度

---

## 开发环境提示
- Node.js + TypeScript（strict）CLI 项目。
- 当前代码仍处于“参考项目导向的重构与演进阶段”，不是最终结构。
- 验证命令：`npm run build`、`npm run typecheck`。
- 运行命令：`npm run start -- "<问题>"`。

---

## 修改边界提示
- 只修改本轮版本目标所必需的文件。
- 未经用户明确要求，不新增依赖。
- 涉及规则变化时，先更新文档，再改代码：
  1. `CONSTRAINTS.md`
  2. `docs/version-plan/*`
  3. 其他说明文档
  4. 代码
- 不要因为“当前代码已经这样写了”就拒绝重构；如果它阻碍版本目标，应优先修正结构。

---

## 文档命名约定
- 普通说明类文档，优先使用中文文件名，降低仓库阅读门槛。
- 但以下“约定型文件名”默认保留，不应随意改名：
  - 根目录 `README.md`
  - `AGENTS.md`
  - 版本文档 `v0.x.y.md`
- 如果未来要调整其它文档文件名，必须同步更新：
  - 所有文档链接
  - 文档优先级说明
  - 版本文档中的交叉引用
  - README 中的入口导航
- 不要为了“全部看起来都是中文”而破坏工具识别、目录入口习惯或版本文档的可排序性。

---

## 质量门禁
- 若 `build/typecheck` 未通过，不得宣称“已完成”。
- 若受环境限制无法验证，必须明确说明：
  - 哪一步未验证
  - 为什么无法验证
  - 用户下一步如何验证
- 若本轮实现明显偏离参考项目主线或版本目标，应主动指出，而不是硬着头皮继续堆功能。

---

## 沟通要求
- 默认中文沟通。
- 涉及重要技术术语时，优先写成“英文术语 + 易懂中文说明”，尤其是第一次出现时。
- 推荐表达方式：
  - `REPL（持续聊天模式）`
  - `Session Storage（恢复最近会话）`
  - `Tool Protocol（工具协议）`
- 避免整段只堆纯英文术语，默认要照顾“能看懂英文词，但更容易先理解中文意思”的阅读习惯。
- 不确定时先说明假设，不隐式猜测。
- 建议必须可执行，避免空泛结论。
- 需要做取舍时，优先给出“为什么这样取舍”和“这与参考项目相比少做了什么”。
