# AGENTS.md

## 目的
本文件用于指导 AI 编码代理（如 Codex）在本仓库内稳定协作。

约束规则来源：`CONSTRAINTS.md`（必须先读并遵守）。

---

## 工作流（最高优先级）
- 不要直接写代码，必须先规划，再编码。
- 默认执行顺序：
  1. 读取上下文（代码、配置、报错、已有约束）。
  2. 给出简明计划（改哪些文件、为什么改、风险点、验证方式）。
  3. 与用户对齐后再编码。
  4. 编码后执行验证并汇报结果。
- 仅当用户明确要求“直接改代码”时，才可跳过规划。

---

## 执行清单（每次任务都做）
1. 先读取 `CONSTRAINTS.md`。
2. 做最小可行改动，避免一次性大范围重构。
3. 优先解决：安全边界、稳定性、可诊断性。
4. 采用“双参考策略”：工程实现参考 Claude Code，产品体验参考 Codex，并说明关键取舍。
5. 修改后必须验证：
   - `npm run build`
   - `npm run typecheck`
6. 编码时严格遵守注释约束：关键逻辑必须写详细注释（尤其是“为什么这样做”）。
7. 汇报格式：先结果，后改动点，最后验证结果与未完成项。

---

## 开发环境提示
- Node.js + TypeScript（strict）CLI 项目。
- 验证命令：`npm run build`、`npm run typecheck`。
- 运行命令：`npm run start -- "<问题>"`。

---

## 修改边界提示
- 只修改实现目标所必需的文件。
- 未经用户明确要求，不新增依赖、不改动项目边界。
- 涉及规则变化时，先更新 `CONSTRAINTS.md`，再改代码。

---

## 质量门禁
- 若 `build/typecheck` 未通过，不得宣称“已完成”。
- 若受环境限制无法验证，必须明确说明：
  - 哪一步未验证
  - 为什么无法验证
  - 用户下一步如何验证

---

## 沟通要求
- 默认中文沟通。
- 不确定时先说明假设，不隐式猜测。
- 建议必须可执行，避免空泛结论。
