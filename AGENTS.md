# AGENTS.md

## 目的
本文件规定 AI 编码代理在仓库中的执行流程。硬约束看 `CONSTRAINTS.md`，学习方法看 `docs/coding/学习协作规则.md`，版本范围看 `docs/version-plan/`。

## 文档优先级
出现冲突时按以下顺序处理：
1. `CONSTRAINTS.md`
2. `AGENTS.md`
3. `docs/coding/学习协作规则.md`
4. `docs/version-plan/版本总览.md`
5. 当前目标版本文档
6. `docs/产品说明.md`
7. `README.md`
8. 现有实现

现有实现不是长期真理。当前工作树是学习基线，应按版本顺序逐步实现，不能从 Git 历史一次性恢复旧答案。

## 每轮工作流
1. 读取 `CONSTRAINTS.md`、版本总览、当前版本文档和产品说明。
2. 判断本轮是 `Learn（学习）`、`Pair（结对）`、`Delegate（委托）` 还是 `Review（审查）`，并在会影响帮助程度时明确说明。
3. 判断是否需要阅读参考仓库。新的核心主题应先让学习者预测，再查阅必要源码。
4. 编码前给出简明计划：文件、职责、风险和验证方式；与用户对齐后再修改。
5. 只实现当前版本所需能力，不提前恢复后续版本结构。
6. 修改后运行 `npm run build`、`npm run typecheck`，以及当前版本要求的测试或关键流程。
7. 汇报顺序：结果、改动、验证、风险、未完成项；学习任务还要汇报最高帮助等级和掌握证据。

用户明确要求“直接改代码”时可以跳过事前对齐，但完成后仍要补充计划取舍、风险和验证结果。

## 学习任务边界
- 不把新的核心学习任务静默转换成完整代写。
- 学习者优先负责核心类型、主要状态变化、一个失败路径以及关键测试。
- AI 可以负责脚手架、重复映射、格式化、文档同步等机械工作。
- 遇到阻塞时按 `H0` 到 `H5` 逐级增加帮助，不直接跳到完整答案。
- 是否掌握以“解释、实现、调试、迁移”四类证据判断，不能只看代码行数或测试通过。

详细流程见 [学习协作规则](./docs/coding/学习协作规则.md)。

## 参考仓库
主参考仓库：`/Users/huzhibin/code/my/ai/claude-code-fork`。

可以借鉴架构分层、执行流程、接口思路、安全模型和交互方式；不得逐行照搬，也不得为追求相似而引入当前版本不需要的复杂度。重大设计前优先查阅对应源码，并说明参考来源、保留内容和主动省略的复杂度。

## 编码与修改边界
- TypeScript 保持 strict，公共与核心函数明确参数和返回类型，不用 `any` 放弃类型检查。
- 关键逻辑写详细中文注释，重点解释职责、原因、不变量和风险。
- 项目内部跨目录导入使用 `#src/*`，不新增多层相对导入。
- 未经明确需求不新增依赖，不修改任务范围外的文件。
- 规则变化先改文档，再改代码。
- 严格遵守 `docs/coding/AI高风险编码规则.md`。

## 沟通要求
默认使用中文。技术术语第一次出现时优先使用“英文术语 + 中文解释”，例如 `QueryLoop（单轮智能体循环）`。不确定时明确说明假设；需要取舍时说明原因以及相比参考项目主动少做了什么。
