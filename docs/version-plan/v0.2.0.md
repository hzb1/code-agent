# v0.2.0

## 版本性质
主版本。

## 版本定位
这是产品第一次从“单次问答原型”升级为“可持续对话的只读代码助手”。

## 本版目标
一次性补齐三组最关键的只读能力：
- 项目检索工具；
- REPL；
- 多轮会话与会话恢复。

## 为什么这一版要把这些放在一起
因为只读代码助手真正可用，至少要同时满足：
- 能找文件；
- 能持续追问；
- 能保留历史；
- 能恢复会话。

如果只做其中一项，用户仍然会觉得它只是一个“命令行问答脚本”。

## 对齐参考仓库的重点
重点参考：
- `claude-code-fork/src/screens/REPL.tsx`
- `claude-code-fork/src/QueryEngine.ts`
- `claude-code-fork/docs/conversation/multi-turn.mdx`
- `claude-code-fork/src/tools.ts`

## 本版要做什么
### 1. 增加检索工具组
新增：
- `src/tools/listFiles.ts`
- `src/tools/searchFiles.ts`

工具职责：
- `list_files`：列目录与结构；
- `search_files`：按关键词找候选文件；
- `read_file`：读取具体文件内容。

这三个工具要能组合成：
- 先列目录；
- 再搜索关键词；
- 再读目标文件；
- 最后基于证据回答。

### 2. 新增 REPL
新增：
- `src/cli/repl.ts`
- `src/cli/output.ts`

至少支持：
- 持续输入；
- `exit` / `quit`；
- `clear`；
- 打印最近对话结果。

### 3. 让 QueryEngine 真正持有会话
`src/app/queryEngine.ts` 至少要持有：
- `messages`
- `sessionId`
- `cwd`
- `model`
- `toolRegistry`
- `readOnlyCache`

### 4. 增加 Session Storage v1
新增：
- `src/session/storage.ts`
- `src/session/serializer.ts`
- `src/session/cache.ts`

第一版即可：
- 存本地 JSON；
- 恢复最近一次会话；
- 不追求复杂 transcript 格式。

### 5. 增加基础 `doctor`
新增：`src/cli/doctor.ts`

至少检查：
- `LLM_*` 配置是否完整；
- provider / base URL / model 是否合理；
- cwd 是否有效；
- 常见网络错误、429、超时建议。

## 建议目录形态
```text
src/
├─ cli/
│  ├─ index.ts
│  ├─ repl.ts
│  ├─ output.ts
│  └─ doctor.ts
├─ app/
│  └─ queryEngine.ts
├─ session/
│  ├─ storage.ts
│  ├─ serializer.ts
│  └─ cache.ts
├─ tools/
│  ├─ readFile.ts
│  ├─ listFiles.ts
│  ├─ searchFiles.ts
│  ├─ registry.ts
│  └─ types.ts
└─ loop/
   └─ queryLoop.ts
```

## 关键代码改动点
重点会动：
- `src/app/queryEngine.ts`
- `src/cli/index.ts`
- `src/tools/registry.ts`
- `src/loop/queryLoop.ts`

重点会新增：
- `src/tools/listFiles.ts`
- `src/tools/searchFiles.ts`
- `src/cli/repl.ts`
- `src/cli/doctor.ts`
- `src/session/*`

## 本版结束后的产品状态
- 产品第一次具备真正的“项目级只读理解能力”；
- 你可以连续追问，而不必每次重新输入背景；
- 结构上也开始出现参考项目那种“会话工具”的感觉。

## 是否已可用
- 对开发者：可用。
- 对最终用户：可以开始内测，但还不稳定。

## 本版主要学习目标
- 理解多轮会话和单轮 Loop 的关系；
- 理解只读工具组为什么是第一批最重要的工具；
- 学会最小可行的 REPL 和会话持久化设计。

## 本版能学到的技术点
- 如何设计 `list_files + search_files + read_file` 这种可组合的只读工具链；
- 如何在 Node.js CLI 里实现最小可用的 REPL、命令解析和交互输出；
- 如何把会话状态从一次调用提升到可持续持有的 Session 模型；
- 如何做本地 JSON 持久化、序列化和恢复，支撑多轮会话；
- 如何为 CLI 增加 `doctor` 诊断入口，把排障能力做成产品功能。

## 本版不要做什么
- 不加写文件；
- 不加命令执行；
- 不加权限弹窗；
- 不加 Plan Mode；
- 不加流式输出。
