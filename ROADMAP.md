# ROADMAP.md

## 文档定位
本文件是基于仓库真实状态整理的“产品路线图”。

说明：
- 若与工程边界冲突，以 `CONSTRAINTS.md` 为准；
- 若与产品定位冲突，以 `PRODUCT.md` 为准。

---

## 路线图总览

```text
阶段1：CLI 最小闭环（已完成）
目标：跑通“提问 -> 读文件 -> 回答”的最小产品闭环。
关键能力：CLI 输入、LLM 调用、`read_file`、基础 Agentic Loop、基础错误处理。

阶段2：稳定化与检索增强（下一阶段）
目标：把“能跑”提升为“更稳、更会找代码”。
关键能力：`list_files`、`search_files`、Agentic Loop 稳定化、诊断能力、错误分层、日志与可观测性。

阶段3：安全改动 CLI
目标：从“只读理解”升级为“可控协助改动”。
关键能力：`write_file`、`exec_command`、确认机制、多轮对话管理、CLI 打字机效果、会话内多步任务。

阶段4：Core 抽离与架构升级
目标：把核心能力从 CLI 外壳中拆出来，为多壳复用做准备。
关键能力：`core / shell` 分层、统一类型、统一工具接口、可复用核心能力。

阶段5：Mac 桌面版 MVP
目标：做出第一个可用的 Mac 图形客户端。
关键能力：桌面壳、项目目录选择、会话界面、任务结果展示、工具过程展示、基础流式输出体验。

阶段6：桌面版增强
目标：把桌面端从“能用”提升到“顺手、像产品”。
关键能力：会话历史、多轮对话管理、任务过程可视化、打字机效果优化、diff 预览、风险弹窗、设置中心。

阶段7：类 Codex 进阶能力
目标：在稳定底座上逐步靠近真正的本地 Coding Agent。
关键能力：Plan 模式、项目级理解、多文件任务、更完整的开发工具链、结果可追溯。
```


---

## 最终目标
最终目标不变：

> 做出一个面向 Web 前端工程师的、本地运行的、类似 Codex 的 Mac 桌面客户端。

它的长期形态应该是：
- 能理解项目本身；
- 能调用本地工具；
- 能完成多步任务；
- 能在安全前提下读文件、搜代码、改代码、执行命令；
- 有清晰的任务过程反馈；
- Desktop 只是壳，核心能力独立存在。

---

## 总体推进原则
- 先做核心，再做外壳；
- 先做 CLI，再做 Desktop；
- 先做稳定与安全，再做“更智能”；
- 先让自己每天愿意用，再考虑“像产品”；
- 每阶段只解决一个核心问题。

---



---

## 各阶段详细路线

## 阶段1：CLI 最小闭环（已完成）

**目标**  
跑通“用户提问 -> 模型决策 -> 读文件 -> 基于证据回答”的最小产品闭环。

**已做成什么**
- CLI 入口；
- 环境变量与 Provider 配置；
- `read_file` 工具；
- 工具注册中心；
- 基础 Agentic Loop；
- build / typecheck 基本验证。

**先不要做什么**
- 不做桌面 UI；
- 不做多文件自动修改；
- 不做命令执行；
- 不做 monorepo。

---

## 阶段2：稳定化与检索增强（下一阶段）

**目标**  
把“能跑”升级为“更稳、更会找代码”。

**具体做什么**
- 增加 `list_files`；
- 增加 `search_files`；
- 把基础 Agentic Loop 打磨成更稳定的执行循环；
- 增加 `doctor` 或类似诊断能力；
- 统一错误分层与日志字段；
- 提升“入口定位、模块定位、调用链定位”的命中率。

**技术点**
- `fs.readdir` / 目录遍历；
- 文本搜索与结果截断；
- 路径安全与搜索范围限制；
- Tool 调度与循环状态管理；
- 调试日志与可观测性设计；
- 配置检查与启动前失败提示。

**功能升级**
- 从“只会读单个文件”升级为“能先找、再读、再答”；
- 从“基础 Agent Loop”升级为“更稳定、可诊断的 Agentic Loop”；
- 从“报错”升级为“可诊断、可操作提示”。

**示例**
```bash
code-agent "帮我找登录逻辑在哪"
code-agent "这个项目入口文件在哪"
code-agent "doctor"
```

**产出**
- `src/tools/listFiles.ts`
- `src/tools/searchFiles.ts`
- `src/cli/doctor.ts` 或同等诊断入口
- 更稳定的 Agentic Loop 主循环
- 更清晰的工具输出格式
- 更稳定的错误提示与 debug 日志

**建议目录演进**
```text
src/
├─ cli/
│  ├─ index.ts
│  └─ doctor.ts
├─ agent/
│  └─ runAgent.ts
├─ core/
│  ├─ config.ts
│  ├─ types.ts
│  └─ errors.ts
├─ llm/
│  └─ chatClient.ts
└─ tools/
   ├─ readFile.ts
   ├─ listFiles.ts
   ├─ searchFiles.ts
   └─ registry.ts
```

**先不要做什么**
- 不做 `write_file`；
- 不做 `exec_command`；
- 不做交互式长期会话；
- 不做桌面端。

---

## 阶段3：安全改动 CLI

**目标**  
让 CLI 从“只读理解”升级为“可控地协助改动和验证”。

**具体做什么**
- 增加 `write_file`；
- 增加 `exec_command`；
- 增加确认流程；
- 增加交互模式或最小 REPL；
- 增加多轮对话管理；
- 增加终端打字机效果（可关闭）；
- 展示修改摘要、命令摘要、失败摘要。

**技术点**
- 文件写入边界与回滚思路；
- `child_process`；
- 命令超时与输出截断；
- 用户确认与风险分级；
- 基础会话状态维护；
- 多轮对话上下文裁剪；
- 终端流式输出与打字机效果控制。

**功能升级**
- 从“解释代码”升级为“先分析，再给出修改，并在确认后执行”；
- 从“单轮问答”升级为“一个会话中完成多步任务”；
- 从“一次性打印结果”升级为“更接近实时反馈的输出体验”。

**示例**
```text
> 给 request 方法加 try-catch
> 帮我跑 typecheck
> 把刚才的改动总结一下
```

**产出**
- `src/tools/writeFile.ts`
- `src/tools/execCommand.ts`
- `src/cli/confirm.ts`
- `src/cli/repl.ts`
- `src/cli/output.ts`
- 会话管理模块
- 更完整的 Agent 工具执行流程

**建议目录演进**
```text
src/
├─ cli/
│  ├─ index.ts
│  ├─ doctor.ts
│  ├─ repl.ts
│  ├─ confirm.ts
│  └─ output.ts
├─ agent/
│  ├─ runAgent.ts
│  └─ session.ts
├─ core/
│  ├─ config.ts
│  ├─ types.ts
│  ├─ errors.ts
│  └─ guards.ts
├─ llm/
│  └─ chatClient.ts
└─ tools/
   ├─ readFile.ts
   ├─ listFiles.ts
   ├─ searchFiles.ts
   ├─ writeFile.ts
   ├─ execCommand.ts
   └─ registry.ts
```

**先不要做什么**
- 不做多文件自动大改；
- 不做复杂 patch 系统；
- 不做桌面端 UI；
- 不做插件机制。

---

## 阶段4：Core 抽离与架构升级

**目标**  
把“核心能力”和“CLI 外壳”彻底拆开，为桌面端做准备。

**具体做什么**
- 抽离 `core`；
- 让 CLI 只负责输入输出；
- 统一事件结构、工具接口、配置接口；
- 为桌面端和未来其它壳复用核心逻辑。

**技术点**
- 模块边界；
- 包拆分；
- 依赖方向控制；
- core / shell 分层。

**功能升级**
- 从“一个单包 CLI 项目”升级为“一个核心 + 多壳”的产品底座。

**建议目录演进**
```text
packages/
├─ core/
│  └─ src/
│     ├─ agent/
│     ├─ core/
│     ├─ llm/
│     ├─ tools/
│     └─ index.ts
├─ cli/
│  └─ src/
│     ├─ index.ts
│     ├─ repl.ts
│     └─ doctor.ts
└─ shared/
   └─ src/
      └─ types.ts
```

**产出**
- 独立可复用的 `core`
- 更轻的 CLI 壳
- 更清晰的边界与依赖关系

**先不要做什么**
- 不急着做精致桌面 UI；
- 不急着做插件系统；
- 不为了“高级”而过度抽象。

---

## 阶段5：Mac 桌面版 MVP

**目标**  
做出第一个能用的 Mac 图形客户端。

**具体做什么**
- 选择并接入桌面壳；
- 做基础会话界面；
- 接入项目目录选择；
- 接入核心 Agent；
- 展示任务结果与工具执行过程；
- 增加基础流式输出体验。

**技术点**
- 桌面容器（Electron 或 Tauri，按实际评估）；
- IPC；
- React 界面；
- 本地状态管理；
- 项目目录选择与权限边界。

**功能升级**
- 从“命令行工具”升级为“桌面可视化入口”；
- 让用户看得见 AI 正在做什么；
- 让用户在桌面端感知到“结果正在生成”。

**建议目录演进**
```text
packages/
├─ core/
├─ cli/
├─ desktop/
│  ├─ electron/ 或 tauri/
│  └─ renderer/
│     └─ src/
│        ├─ App.tsx
│        ├─ components/
│        ├─ pages/
│        └─ store/
└─ shared/
```

**产出**
- 一个可本地运行的 Mac 桌面版 MVP
- 项目选择能力
- 会话输入与结果展示能力
- 工具执行过程展示

**先不要做什么**
- 不先抠视觉；
- 不先做复杂动画；
- 不一开始支持所有平台。

---

## 阶段6：桌面版增强

**目标**  
把桌面端从“能用”打磨到“顺手”。

**具体做什么**
- 会话历史与项目切换；
- 多轮对话管理增强；
- 任务过程可视化；
- 打字机效果与流式输出体验优化；
- 写文件 diff 预览；
- 风险弹窗与中断能力；
- 设置中心。

**技术点**
- 状态同步；
- 本地持久化；
- 会话状态恢复；
- 任务过程建模；
- 交互反馈设计；
- 安全体验设计。

**功能升级**
- 用户能理解 AI 的每一步操作；
- 多轮会话从“能用”升级为“更稳、更自然”；
- 打字机效果从“功能存在”升级为“产品体验增强”；
- 危险能力从“可用”升级为“可控且可感知”。

**产出**
- 更完整的桌面工作流
- 更清晰的执行过程展示
- 更可接受的安全体验

**先不要做什么**
- 不一边做体验一边推倒 core；
- 不盲目叠加功能。

---

## 阶段7：类 Codex 进阶能力

**目标**  
在稳定底座上，逐步向真正的本地 Coding Agent 靠近。

**具体做什么**
- Plan 模式；
- 项目级自动理解；
- 多文件分析与修改；
- 更多开发工具；
- 任务报告与可追溯记录。

**技术点**
- 长上下文控制；
- 任务规划与中断；
- 多工具编排；
- 结果追踪；
- 多步骤执行安全控制。

**功能升级**
- 从“能完成单点任务”升级为“能执行复杂开发任务流”。

**产出**
- 更接近 Codex 风格的桌面端体验
- 更强的本地工程理解能力
- 可展示为长期作品的 Agent 产品

**先不要做什么**
- 不盲目追求全自动；
- 不在基础不稳时堆“智能感”。

---
