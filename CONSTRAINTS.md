# CONSTRAINTS.md

## 说明
本文件是本项目唯一“约束”清单，定义什么能做、什么不能做、做到什么算合格。

---

## 1. 项目边界约束
- 本项目是 CLI Code Agent，不是前端页面项目。
- 只做：用户提问 -> 按需读取本地文件 -> 基于证据回答。
- 未经明确需求，不扩展到写文件、执行命令、联网搜索、多 Agent 协作。

---

## 2. 语言约束
- 默认使用中文表达，适用于：
  - AI 最终输出
  - 代码注释
  - TypeScript 字段/类型描述（如注释、文档说明）
  - 错误提示与日志文案
- 仅在以下情况允许保留英文：
  - 第三方接口原始字段名
  - 标准技术术语（如 `tool_call`、`AbortController`）
  - 命令行命令与环境变量名

---

## 3. 类型约束
- TypeScript 必须保持 strict。
- 禁止新增无必要的 `any`。
- 方法入参和返回值类型必须明确（公共函数/核心函数不得省略）。
- 类型必须可读（避免过深嵌套与难以理解的匿名联合）。

---

## 4. 安全约束
- 文件读取必须限制在项目根目录内，禁止路径越界。
- 文件读取必须有长度上限，超限需显式标记截断。
- Agent 循环必须有上限，禁止无限循环。

---

## 5. 稳定性约束
- LLM 调用必须具备：超时、重试、限速与可读错误提示。
- 错误提示必须可操作（例如：限流、DNS、超时、配置缺失）。

---

## 6. 工程约束
- 小步修改，单次只解决一个主要问题。
- 未经明确需求，不引入新依赖。
- 目录职责保持清晰：`cli`、`agent`、`llm`、`tools`、`core`。
- 约束变更必须先更新本文件。

---

## 7. 验收约束（DoD）
每次改动至少满足：
1. `npm run build` 通过。
2. `npm run typecheck` 通过。
3. CLI 在配置正确时可完成典型问答（如解释 `package.json`、总结项目、定位入口文件）。
