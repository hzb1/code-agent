# CONSTRAINTS.md

## 说明
本文件是本项目唯一“约束”清单，定义什么能做、什么不能做、做到什么算合格。

学习顺序、当前任务和掌握记录不写在本文件中，统一放在 `docs/学习计划/`。

---

## 1. 项目目标约束
- 本项目的目标不是停留在一个小型 `read_file` Demo。
- 本项目的正式目标是：做出一个与本地参考项目 `claude-code-fork` 同类的本地 CLI Coding Agent。
- 工程实现层优先参考：`claude-code-fork`。
- 交互体验层分开参考：
  - 命令行体验优先参考：`claude-code`
  - 桌面端体验在未来阶段参考：Mac 版 `Codex`
- 当前主形态是 CLI，因此当前阶段优先参考 `claude-code` 的命令行体验。
- 现有实现不是必须保留的“既成事实”；如果它阻碍目标达成，可以重构甚至推翻。

当前开发环境中的参考仓库路径：
- `/Users/huzhibin/code/my/ai/claude-code-fork`

关于这个参考仓库，要明确：
- `claude-code-fork` 是一个针对 Anthropic 官方 `Claude Code` CLI 的源码反编译 / 逆向还原项目；
- 因此，本项目所说的“主参考”不是抽象方向参考，而是源码级主参考；
- 后续做架构、流程、工具系统和执行链路时，应优先从这个参考仓库中找对应实现与设计思路。

---

## 2. 学习推进约束
- 学习主线以 `docs/学习计划/学习路线.md` 为准，当前工作以 `docs/学习计划/当前阶段.md` 为准。
- 一次只学习一个核心问题，不得跳过当前阶段提前实现后续大能力。
- 新主题必须包含理解、实现、调试和变化练习，完成证据记录在 `docs/学习计划/学习记录.md`。

## 2.1 AI 协作约束
- 本项目同时追求“做出产品”和“掌握核心原理”，不得用 AI 完成交付来冒充学习完成。
- 新的核心主题默认由学习者负责预测、核心实现、一次失败诊断和最终复述；AI 优先提供问题、提示、局部示例和审查。
- 已掌握的内容、机械性修改和明确要求委托的工作，可以交给 AI 直接实现，但必须显式说明模式切换。
- 构建和测试通过只证明产品行为，不单独证明已经掌握；学习完成还必须能解释、实现、调试并迁移到一个变式。
- 参考仓库用于查证设计与比较取舍。先形成预测，再阅读对应源码；禁止把逐行照抄当作学习。

---

## 3. 语言约束
- 默认使用中文表达，适用于：
  - AI 最终输出
  - 代码注释
  - TypeScript 字段/类型描述（如注释、文档说明）
  - 错误提示与日志文案
  - README / 产品说明 / 学习计划 / 约束文档
- 涉及常见技术术语时，优先使用“英文术语 + 易懂中文说明”的表达方式，尤其是第一次出现时。
- 推荐写法示例：
  - `REPL（持续聊天模式）`
  - `Session Storage（恢复最近会话）`
  - `QueryEngine（会话编排器）`
- 不推荐长期只使用纯英文术语而不给中文解释，避免表达对作者本人不友好。
- 仅在以下情况允许保留英文：
  - 第三方接口原始字段名
  - 标准技术术语（如 `tool_call`、`AbortController`）
  - 命令行命令与环境变量名
  - 参考仓库中的文件名和路径

---

## 4. 类型约束
- TypeScript 必须保持 strict。
- 禁止新增无必要的 `any`。
- 方法入参和返回值类型必须明确。
- 公共函数、核心函数、协议层函数不得省略入参或返回值类型。
- 类型必须可读，避免过深嵌套与难以理解的匿名联合。
- 若引入新的协议层（如 Tool Protocol、Provider Contract、Permission Result），必须先定义清晰类型，再写实现。

---

## 5. 注释约束
- 本项目注释要求“十分详细”。
- 关键模块、关键函数、关键分支必须写清楚：
  - 做了什么
  - 为什么这样做
  - 这一步属于哪一层职责
  - 可能的风险点是什么
- 对参考项目思路的借鉴，如果会影响理解成本，应通过注释说明设计意图。
- 不能只写“表面行为”，必须写“设计原因”。

---

## 6. 安全约束
- 文件读取、写入必须限制在允许的项目边界内。
- 路径处理必须防止越界、路径穿越和未来可能的软链接绕过。
- 文件读取必须有长度上限，超限需显式标记截断。
- Agent 循环必须有上限，禁止无限循环。
- 高风险能力（写文件、执行命令、危险工具）必须经过权限系统或明确确认流程。
- 不得为了追求“像参考项目”而削弱安全边界。

---

## 7. 稳定性约束
- LLM 调用必须具备：超时、重试、限速与可读错误提示。
- 错误提示必须可操作，至少能区分：
  - 配置错误
  - 网络错误
  - 限流错误
  - 超时错误
  - 工具错误
- 会话、上下文、工具执行、权限判断都必须有明确终止条件。
- 不允许把“之后再收口”当作长期借口，涉及可用里程碑的版本必须先稳定再继续。

---

## 8. 工程约束
- 小步修改，单次只解决一个主要问题或当前学习阶段内的一组紧密能力。
- 未经明确需求，不引入新依赖。
- 目录职责必须清晰，但不强绑当前目录名；目录形态以当前学习阶段和架构目标为准。
- 若当前结构阻碍目标实现，应优先调整结构，而不是勉强在旧结构上堆代码。
- 约束变更必须先更新本文件。
- 参考仓库可以作为主参考，但不得逐行照搬源码。

---

## 9. 质量门禁（DoD）
每次改动至少满足：
1. `npm run build` 通过。
2. `npm run typecheck` 通过。
3. 本轮目标对应的关键命令或关键流程能跑通。
4. 文档、约束、学习计划与实现不明显冲突。

---

## 10. 参考实现约束
### 10.1 参考方向
- 工程实现层（内核/架构/流程）优先参考：Claude Code 对应的本地参考仓库 `claude-code-fork`。
- 交互体验层分开参考：
  - 命令行体验优先参考：`claude-code`
  - 桌面端体验在未来阶段参考：Mac 版 `Codex`

### 10.2 参考范围
- 可以参考：
  - 模块划分
  - 执行流程
  - 权限模型
  - 上下文系统
  - 工具系统
  - 流式反馈
  - 可观测性设计
  - 命令行交互方式
  - 未来桌面端的信息架构方向
- 禁止照搬：
  - 源码逐行复制
  - 受限资产与版权内容
  - 当前学习阶段尚不需要的重型实现细节

### 10.3 决策优先级
当出现冲突时，按以下顺序决策：
1. 先满足安全与本项目约束；
2. 再满足当前学习阶段目标；
3. 再对齐参考项目主线；
4. 最后优化当前目标形态的体验：
   - CLI 阶段优先参考 `claude-code`
   - 桌面端阶段再参考 Mac 版 `Codex`

### 10.4 输出要求
- 关键设计变更需说明“参考来源与取舍理由”。
- 若主动推翻现有实现，应说明为什么旧实现不再适合当前目标。
- 最终代码必须符合本项目约束，不因参考而突破边界。
