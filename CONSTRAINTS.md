# CONSTRAINTS.md

## 说明
本文件是本项目唯一“约束”清单，定义什么能做、什么不能做、做到什么算合格。

---

## 1. 项目边界约束
- 本项目是 CLI Code Agent，不是前端页面项目。
- 只做：用户提问 -> 按需读取本地文件 -> 基于证据回答。
- 未经明确需求，不扩展到写文件、执行命令、联网搜索、多 Agent 协作。

---

## 2. 语言约束
- 默认使用中文表达，适用于：
  - AI 最终输出
  - 代码注释
  - TypeScript 字段/类型描述（如注释、文档说明）
  - 错误提示与日志文案
- 仅在以下情况允许保留英文：
  - 第三方接口原始字段名
  - 标准技术术语（如 `tool_call`、`AbortController`）
  - 命令行命令与环境变量名

---

## 3. 类型约束
- TypeScript 必须保持 strict。
- 禁止新增无必要的 `any`。
- 方法入参和返回值类型必须明确（公共函数/核心函数不得省略）。
- 类型必须可读（避免过深嵌套与难以理解的匿名联合）。

---

## 4. 安全约束
- 文件读取必须限制在项目根目录内，禁止路径越界。
- 文件读取必须有长度上限，超限需显式标记截断。
- Agent 循环必须有上限，禁止无限循环。

---

## 5. 稳定性约束
- LLM 调用必须具备：超时、重试、限速与可读错误提示。
- 错误提示必须可操作（例如：限流、DNS、超时、配置缺失）。

---

## 6. 工程约束
- 小步修改，单次只解决一个主要问题。
- 未经明确需求，不引入新依赖。
- 目录职责保持清晰：`cli`、`agent`、`llm`、`tools`、`core`。
- 约束变更必须先更新本文件。
- 本项目注释要求“十分详细”：
  - 关键模块、关键函数、关键分支必须写清楚“为什么这样做”与“风险点”；
  - 不仅写“做了什么”，还要写“为什么这样做”；
  - 新增或修改复杂逻辑时，必须同步补充注释，保证后续可维护性。

---

## 7. 验收约束（DoD）
每次改动至少满足：
1. `npm run build` 通过。
2. `npm run typecheck` 通过。
3. CLI 在配置正确时可完成典型问答（如解释 `package.json`、总结项目、定位入口文件）。

---

## 8. 参考实现约束（双参考策略）

### 8.1 参考方向
- 工程实现层（内核/架构/流程）优先参考：Claude Code。
- 产品体验层（交互/反馈/可用性）优先参考：Codex。

### 8.2 参考范围
- 可以参考：模块划分、执行流程、错误处理思路、可观测性设计、交互范式。
- 禁止照搬：源码逐行复制、私有实现细节、受限资产与版权内容。

### 8.3 决策优先级
- 当“工程实现”与“产品体验”冲突时，按以下优先级决策：
  1. 先满足安全与本项目约束；
  2. 再保证工程可维护性（Claude Code 风格）；
  3. 最后优化体验一致性（Codex 风格）。

### 8.4 输出要求
- 关键设计变更需说明“参考来源与取舍理由”。
- 最终代码必须符合本项目约束，不因参考而突破边界。
